<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bisite.agricoapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginAndRegistrer.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginAndRegistrer.Singup" />
        <activity android:name=".Principal.MainActivity" />
        <activity android:name=".Principal.AddNewCompany" />
        <activity android:name=".Principal.UserLogged" />
        <activity
            android:name=".userInformation.UserInformation"
            android:label="@string/title_activity_user_information"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".userInformation.RecyclerUserInformationListener" />
        <activity android:name=".userInformation.RecyclerItemClickListener" />
        <activity android:name=".datosDeAsociados.MostrarDatosAsociado"></activity>
    </application>

</manifest>